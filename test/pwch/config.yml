---
domain: localhost
url_prefix: /selfservice
assets_path: /pwch/assets/html

server:
  listen_address: 0.0.0.0
  port: 8080

db:
  host: /run/postgresql
  db_name: vmail
  user: vmail
  password: password
  ssl_mode: disable

bcrypt:
  cost: 14

smtp:
  host: localhost
  port: 587
  login_user: noreply@localdomain
  login_password: password
  sender: "PWCH <noreply@localdomain>"

password_policy:
  min_length: 12
  max_length: 238
  lower_case: true
  upper_case: true
  digits: true
  special_char: true

otl:
  valid_for: 10m
